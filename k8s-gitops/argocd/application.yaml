apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: pds-ehealth
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: default
  source:
    repoURL: https://github.com/${GITHUB_USERNAME}/pds-k8s-gitops.git
    targetRevision: main
    path: .
    directory:
      recurse: true
      include: '{base/*.yaml,infrastructure/*.yaml}'
  destination:
    server: https://kubernetes.default.svc
    namespace: pds
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
      allowEmpty: false
    syncOptions:
      - CreateNamespace=true
      - PrunePropagationPolicy=foreground
      - PruneLast=true
    retry:
      limit: 5
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 3m
  revisionHistoryLimit: 10
